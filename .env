# Database Configuration
# These variables will override the defaults in application.properties
# Useful for setting up your production credentials on deployment platforms.

DB_URL=jdbc:mysql://localhost:3306/gemsoul?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=root
